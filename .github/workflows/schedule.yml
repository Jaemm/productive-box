name: Update gist
on:
  push:
    branches:
      - master
  schedule:
    - cron: "0 0 * * *"
jobs:
  update-gist:
    runs-on: ubuntu-latest
    steps:
      - name: Update gist
        uses: maxam2017/productive-box@master
        env:
          GH_TOKEN: ${{ secrets.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 }}
          GIST_ID: ${{ secrets.Jaemm }}
          TIMEZONE: Asia/Seoul
